<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>May node be with you!! And it should refresh by itself</h1>
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">send quote</button>
    </form>
    
    <h2>Quotes</h2>
    <ul class="quotes">
        <% for(let i = 0; i<quotes.length; i++){%>
            <li class="quote">
                <span><%= quotes[i].name %></span>
                <span><%= quotes[i].quote %></span>
            </li>
        <%} %>
    </ul>
    <div>
        <h2>Darth Vedar invates!</h2>
        <p>Replace Gordo's first Quote with a quote written by Darth Vedar</p>
        <button id="update-button">Replace first Quote</button>
    </div>
    <div>
        <h2>Remove Darth Vedar!</h2>
        <p>
            Delete one Darth Vedar Quote. Does nothing if there are no more Darth Vedar quotes.
        </p>
        <button id="delete-button">Delete One</button>
    </div>
    <div>
        <h2>Delete All!!</h2>
        <p>Delete all Quotes and get some new ones!</p>
        <button id="delete-all">Delete All!!</button>
    </div>
    <script src="/main.js"></script>
</body>
</html>